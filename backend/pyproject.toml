[project]
name = "pawait-backend"
version = "0.1.0"
description = "Backend for PawaIt Travel Advisor"

[tool.ruff]
line-length = 120
extend-exclude = ["venv"]

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true
exclude = ["venv"]
